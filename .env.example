# Copy this file to .env.local and fill in real values from your Supabase
# project's Settings > API page. .env.local is git-ignored — never commit
# real keys.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
